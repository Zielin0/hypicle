import { describe, expect, test } from 'vitest';

describe('Endpoints', () => {
  describe('/key', () => {
    test('Deprecated endpoint', async () => {
      expect(true).toBe(true);
    });
  });
});
